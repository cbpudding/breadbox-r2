#ifndef MODEL_H
#define MODEL_H

typedef struct {
	// TODO: Prop stuff
} breadbox_model_t;

int breadbox_model_init(breadbox_model_t *model);
void breadbox_model_fini(breadbox_model_t *model);

#endif